MIZANODOCUMENT 4 OF 4The Master AI Builder Prompt — Session-by-Session Implementation GuidePurposeThis document contains the copy-paste prompts for each build session. Use with Cursor, Windsurf, GitHub Copilot, or any AI coding assistant that can read your project files.Docs LocationPlace all 4 documents in: C:\Users\Kudzanai\Documents\2025\App Developments\Mizano\docs\How to ReferenceIn Cursor or Windsurf: type @docs/filename before your prompt. In chat AI: upload the file directly before pasting the session prompt.Golden RuleOne session = one file. Confirm it works in the browser before starting the next session. Never stack incomplete sessions.PrerequisiteBefore ANY session: StorageManager.js (Session 1) must be complete, tested, and passing in the browser. Everything depends on it.HOW TO USE THIS DOCUMENTEvery prompt in this document is structured in two parts:PART A — The Opening Context Block: Paste this at the start of EVERY session without exception. It tells your AI builder the rules, the architecture, and the constraints it must follow.PART B — The Session Task: Paste this directly after the Context Block in the same message. It tells the AI exactly what to build in this session.After your AI builder responds with the completed code:Save the file to the exact path it tells youAdd the script tag to index.html if instructedTest in the browser using the test steps it providesOnly move to the next session after the browser test passesDo not skip sessions. Do not combine sessions. Each session depends on the previous one.PART A — OPENING CONTEXT BLOCKCopy EVERYTHING below this line and paste it at the very start of every AI builder session. Do not change any of it.═══════════════════════════════════════════════════════════                  MIZANO PLATFORM — SESSION CONTEXT        ═══════════════════════════════════════════════════════════PROJECT ROOT:C:\Users\Kudzanai\Documents\2025\App Developments\MizanoWHAT MIZANO IS:A grassroots sports and community app for Botswana. Mobile-first PWA.Single-page app (index.html) with overlay-based navigation after login.Offline-first: IndexedDB is the primary store. Google Sheets syncs in background.══ LOCKED FILES — ABSOLUTE RULE — DO NOT TOUCH ══index.html   — You may ONLY append &lt;script src=&apos;...&apos;&gt; at the very bottom.login.html   — DESIGN LOCKED. Zero changes. Zero.signup.html  — DESIGN LOCKED. Zero changes. Zero.══ ARCHITECTURE LAWS — ALL MUST BE FOLLOWED ══1. All new features = .js module files injected into existing overlay divs.2. No new HTML pages. No browser navigation away from index.html after login.3. Every IndexedDB write uses window.mizanoStorage.saveEntity() or updateEntity().4. Every entity record must have all 6 sync metadata fields on every write:   local_id, cloud_id, last_modified, sync_status, sync_attempts, created_at5. Medical data (injuries, conditions, allergies) goes to IndexedDB ONLY.   It NEVER appears in any sync payload. Never. Not for any reason.6. Every entity creator can edit their own entity — always.7. Edit mode uses the inline EDITING bar pattern (blue sticky bar at bottom).8. Save = updateEntity() + last_modified + sync_status:&apos;pending&apos; + re-render.9. Cancel = zero writes, all fields revert, EDITING bar disappears.══ PATTERNS TO FOLLOW — ALL MANDATORY ══Form structure   → copy signup.html collapsible section pattern exactly.Form CSS         → use forms.css only. No new CSS files.Overlay inject   → follow TeamBuilder.js injection pattern into #builder-view.Overlay control  → window.MizanoNav.openOverlay(&apos;builder&apos;) / closeOverlay().Back navigation  → window.MizanoNav.back() — never browser history.Sub-panel push   → window.MizanoNav.pushPage(&apos;page-name&apos;).Location cascade → signup_locations.js pattern for all city→area dropdowns.Field names      → MIZANO_DATA_SCHEMA.md is law. Never invent field names.Sport positions  → dropdown_reference_sports_activties_player_positions_data.json.Auth check       → window.MizanoAuth.getCurrentUserId() for current user.Demo mode check  → window.MizanoAuth.isDemo() — block writes if true.══ AFTER EACH FILE — REQUIRED DELIVERABLES ══1. Show me the complete file contents (the full .js file — no truncation).2. Tell me the exact path to save it (relative to project root).3. Tell me if index.html needs a new &lt;script&gt; tag and show the exact line.4. Tell me exactly what to click/do in the browser to verify it works.5. Do not start the next session until I confirm this session passed.══ REFERENCE FILES YOU MUST READ BEFORE CODING ══docs/MIZANO_Data_Schema.md                               → field names (law)docs/EVENT_GENERATION_LOGIC.md                          → event type logicdocs/card_design_schematics.md                          → card border systemassets/css/forms.css                                    → only CSS to useassets/js/TeamBuilder.js                                → overlay inject patternassets/js/signup_locations.js                           → location cascade patterndata/sports_activities_dropdown.js                      → sports listdata/dropdown_reference_botswana_villages_towns.json    → settlementsdata/dropdown_reference_sports_activties_player_positions_data.json → positionsPART B — SESSION PROMPTSAfter pasting the Context Block above, paste the relevant session prompt below it in the same message.SESSION 1 — StorageManager.js  THIS SESSION MUST BE COMPLETED AND BROWSER-TESTED BEFORE ANY OTHER SESSION. Nothing works without it.SESSION 1 TASK: Update StorageManager.jsOpen the existing StorageManager.js file in the project root.Read the complete file first. Then ADD the following — do not remove any existing code.STEP 1 — Add these IndexedDB object stores in the db.onupgradeneeded handlerif they do not already exist. Every store needs all 6 sync metadata fields.  &apos;groups&apos;        — indexes: by_admin_uid, by_sport, by_area  &apos;associations&apos;  — indexes: by_admin_uid, by_sport, by_scope  &apos;venues&apos;        — indexes: by_city, by_type, by_bookable  &apos;businesses&apos;    — indexes: by_owner_uid, by_type, by_city  &apos;player_files&apos;  — indexes: by_user_uid, by_sport  &apos;minors&apos;        — indexes: by_guardian_uid, by_school  &apos;medical_records&apos; — indexes: by_user_uid (NEVER synced — device only)STEP 2 — Add or update window.mizanoStorage with these exact methods:  saveEntity(storeName, data)    → auto-adds all 6 sync metadata fields before saving    → returns Promise&lt;local_id&gt;  updateEntity(storeName, localId, updates)    → merges updates into existing record    → auto-sets last_modified = Date.now() and sync_status = &apos;pending&apos;    → returns Promise&lt;void&gt;  getEntitiesByUser(storeName, userId)    → returns Promise&lt;Array&gt; filtered by owner_uid === userId    → also includes records where userId in members[] or admins[]  getEntityById(storeName, localId)    → returns Promise&lt;Object&gt; single record by primary key  getProfile(userId)    → reads users store + user_profiles store, returns merged Promise&lt;Object&gt;  saveProfile(profileData)    → writes to both users and user_profiles stores in one transaction    → returns Promise&lt;void&gt;  getPendingSync(storeName)    → returns Promise&lt;Array&gt; — records with sync_status: &apos;pending&apos;  markSynced(storeName, localId, cloudId)    → sets sync_status: &apos;synced&apos; and stores cloud_id    → returns Promise&lt;void&gt;  logout()    → clears auth session from LocalStorage only    → IndexedDB data is KEPT on device — do not delete it    → returns Promise&lt;void&gt;STEP 3 — The _addSyncMeta() internal method that saveEntity() must use:  { ...data, local_id: undefined (auto-increment), cloud_id: null,    last_modified: Date.now(), sync_status: &apos;pending&apos;,    sync_attempts: 0, created_at: Date.now() }BROWSER TEST: Open index.html → Open DevTools → Application → IndexedDBVerify you can see all new stores listed: groups, associations, venues,businesses, player_files, minors, medical_recordsSESSION 2 — ProfilePanel.jsSESSION 2 TASK: Create ProfilePanel.jsCreate a new file: ProfilePanel.js in the project root.This module populates #mine-panel-content inside Panel 8 of index.html.It is the first thing a user sees after signing up.ProfilePanel.init() must:  1. Call window.MizanoAuth.getCurrentUserId() to get the current userId.  2. Call window.mizanoStorage.getProfile(userId) to load profile data.  3. Render the Identity Hero (always visible, non-collapsible):     - Circular avatar 80px (initials fallback if no photo)     - Full name (bold, large)     - @username (grey, smaller)     - City and Area chip     - Top 4 interest chips     - Profile completeness bar (0-100%, calculated from filled fields)     - &apos;Edit Profile&apos; button (top right)  4. Render 8 sub-section cards below the hero:     My Identity / My Player Files / My Groups / My Associations /     My Events / My Venues / My Business / My Minors  5. Each card shows: icon + title + empty-state text OR populated summary.  6. Each card tap opens the correct sub-panel module:     My Player Files → MyPlayerFiles.init()     My Groups → MyGroups.init()     [etc — stub with console.log for now — real modules come later]&apos;Edit Profile&apos; button behaviour:  - Blue sticky EDITING bar slides in at bottom: [Save Changes] [Cancel]  - All hero fields become editable inputs inline  - Save → validate → window.mizanoStorage.saveProfile(data) → re-render → toast  - Cancel → revert all fields → no writeDEMO MODE: If window.MizanoAuth.isDemo() is true:  - Load data from demo_profile.js  - Show banner: &apos;Demo Profile — Sign up to save your content&apos;  - &apos;Edit Profile&apos; button shows Sign Up modal instead of edit mode  - All sub-section card taps show Sign Up modal instead of sub-panelsAdd to index.html: &lt;script src=&apos;./ProfilePanel.js&apos;&gt;&lt;/script&gt;Call ProfilePanel.init() inside the DOMContentLoaded init block.BROWSER TEST: Sign up on signup.html. After redirect to index.html,Panel 8 (Mine) must show your name in the hero and 8 cards below.SESSION 3 — AddActionRouter.jsSESSION 3 TASK: Create AddActionRouter.jsCreate a new file: AddActionRouter.js in the project root.This module replaces the placeholder toggleAddActionPanel() function.BOTTOM SHEET BEHAVIOUR:  - Tapping #btn-add: overlay fades in, #add-action-panel slides up (CSS, 280ms)  - Sheet shows: drag handle + 7 option rows + Cancel row  - Each option: icon (text emoji is fine) + bold label + grey description  - Tapping overlay or Cancel: sheet slides down, overlay fades outTHE 7 OPTIONS (in this order):  1. Add Minor — &apos;Register a child profile&apos;  2. Add Event — &apos;Create a match, session, or tournament&apos;  3. Add Group — &apos;Start or register a team or club&apos;  4. Add Association — &apos;Register a governing body or league&apos;  5. Add Venue — &apos;Submit a pitch, court, gym, or hall&apos;  6. Add Business — &apos;List a shop, service, or corporate sponsor&apos;  7. Add Player File — &apos;Create your athletic profile and CV&apos;DEMO MODE: If window.MizanoAuth.isDemo() is true:  - Tapping any option closes the sheet and shows the Sign Up modal.  - Do not call openForm() in demo mode.openForm(formType) MUST:  1. Clear #builder-view innerHTML  2. Inject sticky header: back button + form title  3. For now, stub all 7 forms with a placeholder message:     &apos;&lt;div style=&quot;padding:20px&quot;&gt;&lt;p&gt;[FormName] form coming soon&lt;/p&gt;&lt;/div&gt;&apos;  4. Call window.MizanoNav.openOverlay(&apos;builder&apos;)  5. Call attachCollapsibleListeners() to re-attach after DOM injectionAdd to index.html: &lt;script src=&apos;./AddActionRouter.js&apos;&gt;&lt;/script&gt;BROWSER TEST: Tap the + button. Sheet slides up with 7 options.Tap each option — #builder-view opens with stub message. Back button closes it.SESSIONS 4–10 — The 7 Forms (One File Per Session)  Use this exact template for each of the 7 form sessions. Copy, replace the form-specific parts shown in [BRACKETS], and append to the Context Block.SESSION [N] TASK: Create [FormName].jsCreate a new file: [FormName].js in the project root.[FormName].render() returns an HTML string injected by AddActionRouter.openForm().Read the complete field specification for this form from:docs/MIZANO_DOC2_Profile_and_7Forms.docx — Part [N] of that document.Implement every field, every section, every validation listed there.COLLAPSIBLE SECTIONS:  - Follow signup.html collapsible section pattern exactly.  - Each section: header tap → section body expands/collapses.  - All sections start COLLAPSED on first render.  - Required sections have a red asterisk on the header if unfilled.LOCATION FIELDS:  - All city→area dropdowns: follow signup_locations.js cascade pattern.  - City select populates area select on change.ON FORM SUBMIT ([Submit Button Label]):  1. Validate all required fields — highlight failed fields red with error text.  2. If FAIL: scroll to first error, do not submit.  3. If PASS: build entity object with correct field names from MIZANO_DATA_SCHEMA.md.  4. Call window.mizanoStorage.saveEntity(&apos;[storeName]&apos;, entityObject).  5. Show toast: &apos;[Success message]&apos;  6. Call window.MizanoNav.closeOverlay(&apos;builder&apos;).  7. Call [SubPanelModule].refresh() if that module exists and is loaded.DEMO MODE: If window.MizanoAuth.isDemo() is true, show Sign Up modal on submit.  Do not write to IndexedDB in demo mode.After building [FormName].js, update AddActionRouter.js:  Replace the stub for &apos;[formType]&apos; case with: [FormName].render()Add to index.html: &lt;script src=&apos;./[FormName].js&apos;&gt;&lt;/script&gt;(Before AddActionRouter.js script tag, after StorageManager.js)BROWSER TEST: Tap + → tap &apos;[Option Name]&apos; → form opens with all sections.Fill all required fields → tap submit → check IndexedDB for the new record.Open DevTools → Application → IndexedDB → &apos;[storeName]&apos; store.SESSION-BY-SESSION FORM SUBSTITUTIONSSESSION 4 — MinorForm.js | Store: minors | Submit: &apos;Save Minor Profile&apos; | Subpanel: MyMinorsSESSION 5 — EventForm.js | Store: activities | Submit: &apos;Create Event&apos; | Subpanel: MyEventsSESSION 6 — GroupForm.js | Store: groups | Submit: &apos;Create Group&apos; | Subpanel: MyGroupsSESSION 7 — AssociationForm.js | Store: associations | Submit: &apos;Register Association&apos; | Subpanel: MyAssociationsSESSION 8 — VenueForm.js | Store: venues | Submit: &apos;Submit Venue&apos; | Subpanel: MyVenuesSESSION 9 — BusinessForm.js | Store: businesses | Submit: &apos;List Business&apos; | Subpanel: MyBusinessSESSION 10 — PlayerFileForm.js | Store: player_files | Submit: &apos;Save Player File&apos; | Subpanel: MyPlayerFiles  For Session 10 (PlayerFileForm.js) only: After the standard template above, add: &apos;Sport selection must dynamically reload the positions chip selector from dropdown_reference_sports_activties_player_positions_data.json. When the sport changes, the positions section re-renders with sport-specific positions.&apos;SESSION 11 — PromoBuilder.jsSESSION 11 TASK: Create PromoBuilder.jsCreate a new file: PromoBuilder.js in the project root.This sub-panel opens from the Business Card &apos;Push a Promo&apos; chip.It injects into #builder-view using AddActionRouter&apos;s openForm pattern.BUILD A 5-STEP FLOW with a step indicator at the top (Step 1 of 5, etc.):  Step 1 — Channel: Two large option cards:    [Show on Business Profile] or [List on Shopping Panel (Panel 11)]  Step 2 — Media: Image or video upload. Preview shown after selection.  Step 3 — Headline: Text input (max 80 chars). Character counter.  Step 4 — Offer Details: Price/discount freetext + optional original/new price.           If both prices given: auto-calculate and display % discount.  Step 5 — Expiry: Date picker. Default 7 days from today. Shows &apos;Active for X days&apos;.Each step: [Back] chip and [Next] chip at the bottom. Cannot advance withoutcompleting the current step&apos;s required fields.ON &apos;PUBLISH PROMO&apos; (final step submit):  1. Build promo object: { headline, offer_text, channel, expiry_date, media_url }  2. Fetch the business record from businesses store.  3. Push the promo object into business.promotions[] array.  4. Call window.mizanoStorage.updateEntity(&apos;businesses&apos;, businessLocalId, { promotions })  5. If channel is &apos;shopping&apos;: create a card-promo entry in a promos store (or Panel 11 data).  6. Toast: &apos;Promo is live!&apos;  7. Close overlay. Business Card Promotions section refreshes.PromoBuilder.open(businessLocalId) must be callable from MyBusiness.js.Add to index.html: &lt;script src=&apos;./PromoBuilder.js&apos;&gt;&lt;/script&gt;SESSIONS 12–18 — Mine Sub-Panels (One File Per Session)  Each sub-panel follows the same base structure. Use this template, replacing the [BRACKETED] parts.SESSION [N] TASK: Create [ModuleName].jsCreate a new file: [ModuleName].js in the project root.[ModuleName].init() opens a sub-panel inside the active overlay.Read the complete spec for this sub-panel from:docs/MIZANO_DOC3_DetailViews_EditLoops_Navigation.docx — Part [N].REQUIRED STRUCTURE:  1. Sticky header: back chevron (calls MizanoNav.back()) + title + [+ Create] button  2. Filter chips relevant to this entity type  3. Load entities: window.mizanoStorage.getEntitiesByUser(&apos;[storeName]&apos;, userId)  4. Empty state: illustration placeholder + prompt text + [Create] chip  5. Populated state: list of closed entity cards  6. Each closed card taps to expand inline (collapsible sections pattern)  7. Expanded card sections: as specified in Document 3 for this entity type  8. Edit chip (owner/admin only): triggers Universal Edit Pattern:       - Blue EDITING bar at bottom       - Fields become inputs       - Save → updateEntity() → re-render       - Cancel → revert, no write[ModuleName].refresh() must re-query IndexedDB and re-render the list.Called by the corresponding Form module after a successful save.After building [ModuleName].js, update ProfilePanel.js:  Replace the console.log stub for this sub-panel with [ModuleName].init()Add to index.html: &lt;script src=&apos;./[ModuleName].js&apos;&gt;&lt;/script&gt;BROWSER TEST: Go to Mine panel. Tap &apos;[Card Name]&apos; card.Sub-panel opens with header and filter chips.Tap &apos;+ Create&apos; — correct form opens. Fill and submit. Return to sub-panel.New entity appears in the list. Tap to expand. Edit chip visible.Tap Edit — EDITING bar appears. Change a field. Save — field updates. No page reload.SESSION-BY-SESSION SUB-PANEL SUBSTITUTIONSSESSION 12 — MyGroups.js | Store: groups | Part 14 in Doc 3 | + Create → GroupFormSESSION 13 — MyEvents.js | Store: activities | Part 16 in Doc 3 | + Create → EventFormSESSION 14 — MyBusiness.js | Store: businesses | Part 17 in Doc 3 | + Create → BusinessFormSESSION 15 — MyPlayerFiles.js | Store: player_files | Part 18 in Doc 3 | + Create → PlayerFileFormSESSION 16 — MyAssociations.js | Store: associations | Part 15 in Doc 3 | + Create → AssociationFormSESSION 17 — MyVenues.js | Store: venues | Part 19 in Doc 3 | + Create → VenueFormSESSION 18 — MyMinors.js | Store: minors | Part 20 in Doc 3 | + Create → MinorForm  For Session 14 (MyBusiness.js) only: After the standard template, add: &apos;Also build the full 10-section expanded Business Card as described in Part 17 of Document 3. This includes Linked Profiles, Sponsorships, Analytics (placeholder charts), Promotions (calls PromoBuilder.open()), and Billing sections. This is the most complex module in the platform.&apos;SESSION 19 — CardRenderer.js (13 Missing Cards)SESSION 19 TASK: Add 13 card templates to CardRenderer.jsOpen CardRenderer.js. Read it completely first.ADD 13 new card template functions. Do NOT modify existing functions.Read the full card specs from:docs/MIZANO_DOC3_DetailViews_EditLoops_Navigation.docx — Part 23.docs/card_design_schematics.md — for the 7-colour border system.ADD THESE FUNCTIONS (each returns an HTML string):  renderPlayerProfileCard(data)  renderLeaderboardCard(data)  renderPromoCard(data)  renderVenueCard(data)  renderLessonCard(data)  renderHobbyCard(data)  renderLeisureCard(data)  renderSpotlightCard(data)  renderSchoolCard(data)  renderCommunityActionCard(data)  renderServiceCard(data)  renderRecruitmentCard(data)  renderMinorCard(data)Each card tap must call the appropriate detail view open function.Use the 7-colour border system from card_design_schematics.md.BROWSER TEST: Open Panel 1 (Sports). If player files exist with scouting ON,card-playerprofile cards appear. If not, write a test render call in the console.Verify each new card renders without breaking any existing card types.SESSION 20 — Final Integration TestRun through this complete checklist AFTER all 19 sessions are complete. Every item must pass before the platform is ready to share with schools, corporates, and users.FINAL INTEGRATION TEST CHECKLIST — ALL MUST PASS1. Signup → login.html → signup.html → fills form → redirected to index.html → Mine panel shows name and hero2. Tap + button → bottom sheet slides up with 7 options visible3. Create a Group → appears in My Groups sub-panel AND in Panel 6 (Groups) feed4. Create an Event → appears in My Events sub-panel AND in Panel 5 (Events) feed5. Create a Business → appears in My Business sub-panel AND in Panel 12 (Shops) feed6. Create a Player File → appears in My Player Files AND in Panel 1 (Sports) if scouting ON7. Tap Edit on a Group → EDITING bar appears → change name → Save → name updates in-place. No reload.8. Tap Venue chip inside an Event card → Venue Detail sub-panel opens → Back chevron returns to Event9. Log out → redirected to login.html → log back in → all data still there (IndexedDB persists)10. Add a Minor → appears in My Minors → &apos;Enter in Event&apos; chip filters events by minor&apos;s age category11. Business owner adds staff profile → monthly fee updates from P0 to P10 instantly12. Browse mode: tap + button → Sign Up modal appears — NO form opens, NO IndexedDB write13. All 13 new card types render in their correct panels without breaking existing cards14. School signs up → creates Association → creates Groups → invites students → all entities linked15. Corporate signs up → creates Business → creates corporate team → enables tournament interest toggle16. Create Event with entry fee → join as another user → roster entry written to match_rosters store17. Profile completeness bar increases as more sections are filled18. Medical data: create a Player File with injury logged → verify medical_records store has it → verify sync payload does NOT include itQUICK RESET CARD — Paste This If Your AI Builder Gets ConfusedIf your AI builder starts going off-course mid-session, paste this reset card after your Context Block to re-anchor it:══ MIZANO ARCHITECTURE RESET — READ BEFORE CONTINUING ══NAVIGATION: User never leaves index.html after login. Browser history is never used.All views open in: #builder-view (forms) or #detail-view (entities).Back button = window.MizanoNav.back(). Not history.back().DATA WRITES: window.mizanoStorage.saveEntity(storeName, data)DATA READS:  window.mizanoStorage.getEntitiesByUser(storeName, userId)DATA UPDATE: window.mizanoStorage.updateEntity(storeName, localId, updates)OVERLAYS: window.MizanoNav.openOverlay(&apos;builder&apos;)  → opens #builder-view          window.MizanoNav.closeOverlay(&apos;builder&apos;) → closes #builder-view          window.MizanoNav.pushPage(&apos;page-name&apos;)   → push sub-panel on stackFORMS: Use forms.css only. Collapsible sections from signup.html pattern.EDIT:  Blue EDITING bar at bottom. Save = updateEntity + re-render. Cancel = revert.DEMO:  window.MizanoAuth.isDemo() = true → show Sign Up modal, do NOT write.MEDICAL: Never sync. Never show to other users. IndexedDB only.LOCKED:  index.html / login.html / signup.html — zero structural changes.MIZANO Platform — Document Suite 4 of 4 — Complete Implementation Guide
